language: android
dist: precise

jdk:
  - oraclejdk8

sudo:
  required

env:
  matrix:
    - ANDROID_TARGET=android-24 ANDROID_ABI=armeabi-v7a
    #- ANDROID_TARGET=android-24 ANDROID_ABI=x86
  global:
    # wait up to 10 minutes for adb to connect to emulator
    - ADB_INSTALL_TIMEOUT=20

android:
    components:
        - tools
        - build-tools-25.0.2
        - android-24
        - extra-google-google_play_services
        - extra-google-m2repository
        - extra-android-m2repository

    licenses:
        - 'android-sdk-license-.+'
        - 'google-gdk-license-.+'

before_cache:
    - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
    bundler: true
    directories:
        - $HOME/.gradle/caches/
        - $HOME/.gradle/wrapper/
        - node_modules
        - $HOME/ve
        #- $HOME/docker

before_install:
  # Download SDK manually
#  - wget https://dl.google.com/android/android-sdk_r24.4.1-linux.tgz -O $HOME/android-sdk.tgz
#  - tar -xzf $HOME/android-sdk.tgz -C $HOME
#  - export ANDROID_HOME=$HOME/android-sdk-linux
#  - export PATH=$PATH:$ANDROID_HOME/tools:$ANDROID_HOME/tools/bin:$ANDROID_HOME/platform-tools

#  - sudo touch "$HOME/.android/repositories.cfg"
#  - sudo bash -c 'sudo echo -e "count=0" >> "$HOME/.android/repositories.cfg"'
  - sudo mkdir $ANDROID_HOME/licenses
  - sudo touch "$ANDROID_HOME/licenses/android-sdk-license"
  - sudo touch "$ANDROID_HOME/licenses/android-sdk-preview-license"
  - sudo bash -c 'sudo echo -e "\n8933bad161af4178b1185d1a37fbf41ea5269c55" >> "$ANDROID_HOME/licenses/android-sdk-license"'
  - sudo bash -c 'sudo echo -e "\n84831b9409646a918e30573bab4c9c91346d8abd" >> "$ANDROID_HOME/licenses/android-sdk-preview-license"'

before_script:
  - chmod +x gradlew
  - sudo chmod u+x createPassfile
  - sudo ./createPassfile -pwd $KEYSTORE_PASS;

#  - echo y | $ANDROID_HOME/tools/android update sdk --no-ui --all --filter "tools, platform-tools"
#  - echo y | $ANDROID_HOME/tools/bin/sdkmanager "platforms;android-24"
#  - echo y | $ANDROID_HOME/tools/bin/sdkmanager "system-images;android-22;default;armeabi-v7a"
#  - echo no | android create avd --force -n test -t android-22 --abi armeabi-v7a
#  - mksdcard -l testsdcard 1024M sdcard.img
#  - emulator -avd test -sdcard sdcard.img -no-audio -no-window &
#  - echo y | $ANDROID_HOME/tools/bin/sdkmanager "build-tools;25.0.2"
#  - echo y | $ANDROID_HOME/tools/bin/sdkmanager "cmake;3.6.3155560"
#  - echo y | $ANDROID_HOME/tools/bin/sdkmanager "lldb;2.2"
#  - echo y | $ANDROID_HOME/tools/bin/sdkmanager "ndk-bundle"
#  - export ANDROID_NDK_HOME=$ANDROID_HOME/ndk-bundle

script:
    - if [ $TRAVIS_BRANCH == "release_candidate" ]; then ./gradlew assembleStagingRelease crashlyticsUploadDistributionStagingRelease; else ./gradlew assembleStagingDebug --debug; fi